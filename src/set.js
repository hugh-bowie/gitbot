const path = require('path');
const fs = require('fs');
const url = require('url');

// let giturl = new URL('https://gitlab.com/dfagang/Purdue/nalmefene/nalmefene-emails/rep-triggered-emails-copper-greyhound/-/issues/11');

// console.log(`giturl.hash: ${giturl.hash}`);
// console.log(`giturl.host: ${giturl.host}`);
// console.log(`giturl.href: ${giturl.href}`);
// console.log(`giturl.port: ${giturl.port}`);


// //remove duplicates
// //const items = [...(new Set(links))];

// console.log(path.basename(__filename));